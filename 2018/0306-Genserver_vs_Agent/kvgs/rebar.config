{erl_opts, [debug_info]}.
{deps, []}.
{relx, [
    {release, {kvgs, "0.1.0"},
     %% list of apps to include
     [kvgs, observer]},

    %% Don't ship an Erlang VM by default
    {include_erts, false}
]}.
